# This file should include credentials for performing live integration/acceptance tests against a CI API account
# Copy as test/config.yml (which is .gitignored) and edit.
:uri: https://api.cissme.com/media/v1
:username: foo
:password: bar
# A UPC which exists in the CI API account above
:test_upc: "0123456789012"
# An organisation_id that exists in the CI API and that owns the UPC above
:test_organisation_id: "3704196910016"
# An audio file which exists in the CI API account above
:test_audio_file: "327944839"